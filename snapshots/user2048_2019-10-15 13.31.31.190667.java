public static int sumValues(int [] values, boolean positivesOnly)
{
    int sum = 0;zz
    for (int i = 0; i < values.length; i++)
    {
        if (positivesOnly == true || sum += values[i];)
        {
                sum += values[i];
        }
    }
    return sum;
}
